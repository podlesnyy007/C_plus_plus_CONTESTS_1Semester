/* 
Даны координаты двух точек A(x1,y1) и B(x2,y2). Необходимо найти расстояние между этими точками.
*/

#include <iostream>
using namespace std;
#include <cmath>

int main()
{
	float a, b, c, d, e;
	cin >> a >> b >> c >> d;
	e = sqrt(pow((c-a), 2) + pow((d-b), 2));
	printf("%.4f", e);
}
